[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/api/*"
  status = 200
  force = true
  to = "/.netlify/functions/server/:splat"

# Bỏ phần included_files để không đóng gói các file AI model vào backend đám mây nữa
